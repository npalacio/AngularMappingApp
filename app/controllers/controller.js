angular.module("app").controller("controller", ["$scope", "$window", function($scope, $window){
    // var mapContainer = document.querySelector("#mapContainer");
    // mapContainer.width = $window.innerWidth - 300;
    // angular.element($window).bind('resize', function() {
        // console.log($window);
        // console.log($window.innerWidth - angular.element(".listContainer").width);
        // angular.element("#mapContainer").width = $window.innerWidth - angular.element(".listContainer").width;
        // $scope.mapWidth = $window.innerWidth - 300;
        // var mapContainer = document.querySelector("#mapContainer");
        // mapContainer.width = $window.innerWidth - 300;
        // mapContainer.width($window.innerWidth - 300);
        // $("#mapContainer").width = $window.innerWidth - 300;
    // });
}]);
